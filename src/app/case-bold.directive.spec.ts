import { CaseBoldDirective } from './case-bold.directive';

describe('CaseBoldDirective', () => {
  it('should create an instance', () => {
    const directive = new CaseBoldDirective();
    expect(directive).toBeTruthy();
  });
});
